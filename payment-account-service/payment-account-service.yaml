---
apiVersion: v1
kind: Service
metadata:
  name: payment-account-service
  labels:
    app: demo-app
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: demo-app
    tier: compute
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: payment-account-service
  labels:
    app: demo-app
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: demo-app
        tier: compute
    spec:
      containers:
        - image: docker.io/mrudul03/payment-account-service:01
          imagePullPolicy: Always
          name: payment-account-service
          env:
            - name: MYSQL_DB_USER
              value: demouser
            - name: MYSQL_DB_PASSWORD
              value: password
            - name: MYSQL_DB_HOST
              value: demo-database
            - name: MYSQL_DB_PORT
              value: "3306"
          ports:
            - containerPort: 8080